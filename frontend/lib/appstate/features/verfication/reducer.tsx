import { createReducer } from "@reduxjs/toolkit";

export type VerficationState = {};

const initialState: VerficationState = {};

export const VerificationReducer = createReducer(initialState, (builder) => {});
